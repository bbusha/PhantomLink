<UserControl x:Class="PhantomLink.GUI.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Margin="5">
            <Button Content="Clear" Click="Clear_Click" Margin="2"/>
            <Button Content="Save to File" Click="SaveToFile_Click" Margin="2"/>
            <Separator/>
            <CheckBox x:Name="GameLogsCheck" Content="Game Logs" IsChecked="True" Margin="2"/>
            <CheckBox x:Name="AutoScrollCheck" Content="Auto Scroll" IsChecked="True" Margin="2"/>
            <Separator/>
            <CheckBox x:Name="ShowInfoCheck" Content="Info" IsChecked="True" Margin="2"/>
            <CheckBox x:Name="ShowWarningsCheck" Content="Warnings" IsChecked="True" Margin="2"/>
            <CheckBox x:Name="ShowErrorsCheck" Content="Errors" IsChecked="True" Margin="2"/>
        </ToolBar>
        
        <!-- Log Text Box -->
        <TextBox x:Name="LogTextBox" Grid.Row="1" Margin="5"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"/>
        
        <!-- Status -->
        <StatusBar Grid.Row="2">
            <TextBlock x:Name="LogStatus" Text="Log ready"/>
        </StatusBar>
    </Grid>
</UserControl>
