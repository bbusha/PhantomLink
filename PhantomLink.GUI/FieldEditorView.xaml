<UserControl x:Class="PhantomLink.GUI.FieldEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Type Selection -->
        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal">
            <TextBlock Text="Type Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="ObjectAddressBox" Width="200" Margin="0,0,5,0"/>
            <Button Content="Load" Click="InspectObject_Click" Margin="0,0,5,0"/>
            <Button Content="Refresh" Click="RefreshFields_Click"/>
        </StackPanel>
        
        <!-- Object Info -->
        <StackPanel Grid.Row="1" Margin="5" Orientation="Horizontal">
            <TextBlock x:Name="ObjectTypeText" Text="Type: Not inspected" Margin="0,0,10,0"/>
            <TextBlock x:Name="FieldCountText" Text="Fields: 0"/>
        </StackPanel>
        
        <!-- Fields Grid -->
        <DataGrid x:Name="FieldsGrid" Grid.Row="2" Margin="5"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Field Name" Binding="{Binding FieldName}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Type" Binding="{Binding FieldType}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="200"/>
                <DataGridCheckBoxColumn Header="Writable" Binding="{Binding CanWrite}" Width="60" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Actions" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Update" Click="UpdateField_Click" 
                                    IsEnabled="{Binding CanWrite}" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Status -->
        <StatusBar Grid.Row="3">
            <TextBlock x:Name="FieldStatus" Text="Enter a full type name and click Load"/>
        </StatusBar>
    </Grid>
</UserControl>
