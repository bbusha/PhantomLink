<UserControl x:Class="PhantomLink.GUI.SceneBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal">
            <Button Content="Refresh" Width="100" Margin="0,0,10,0" Click="Refresh_Click"/>
            <Button Content="Reload Scene" Width="130" Margin="0,0,10,0" Click="Reload_Click"/>
            <Button Content="Active Objects" Width="130" Click="ActiveObjects_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="5">
            <TextBlock x:Name="ActiveSceneText" FontWeight="Bold"/>
            <TextBlock x:Name="SceneSummaryText" Foreground="{DynamicResource PL.TextSecondaryBrush}"/>
            <TextBlock x:Name="ActiveObjectsText" Foreground="{DynamicResource PL.TextSecondaryBrush}"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Margin="5" Header="Loaded Scenes">
            <ListBox x:Name="ScenesList"/>
        </GroupBox>

        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Foreground="{DynamicResource PL.TextSecondaryBrush}" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
