<UserControl x:Class="PhantomLink.GUI.SceneHierarchyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal">
            <TextBlock Text="Scene:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="SceneCombo" Width="220" Margin="0,0,10,0"/>
            <Button Content="Refresh Scenes" Width="130" Margin="0,0,10,0" Click="RefreshScenes_Click"/>
            <Button Content="Load Roots" Width="110" Margin="0,0,10,0" Click="LoadRoots_Click"/>
            <Button Content="Inspect Selected" Width="140" Click="InspectSelected_Click"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Margin="5" Header="Search">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <TextBlock Text="Name/Id:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="SearchBox" Width="260" Margin="0,0,10,0"/>
                    <Button Content="Find" Width="90" Margin="0,0,10,0" Click="Find_Click"/>
                    <Button Content="Jump" Width="90" Margin="0,0,10,0" Click="Jump_Click"/>
                    <Button Content="Clear" Width="90" Click="ClearSearch_Click"/>
                    <TextBlock x:Name="SearchCountText" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource PL.TextSecondaryBrush}"/>
                </StackPanel>

                <ListBox Grid.Row="1" x:Name="ResultsList" Height="90" DisplayMemberPath="DisplayText" SelectionChanged="ResultsList_SelectionChanged"/>
            </Grid>
        </GroupBox>

        <TreeView Grid.Row="2" x:Name="Tree" Margin="5" SelectedItemChanged="Tree_SelectedItemChanged">
            <TreeView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Inspect" Click="InspectSelected_Click"/>
                    <MenuItem Header="Copy Full Path" Click="CopySelectedPath_Click"/>
                    <Separator/>
                    <MenuItem Header="Pin Object" Click="PinSelected_Click"/>
                    <MenuItem Header="Unpin Object" Click="UnpinSelected_Click"/>
                    <Separator/>
                    <MenuItem Header="Dump to JSON" Click="DumpSelected_Click"/>
                </ContextMenu>
            </TreeView.ContextMenu>
        </TreeView>

        <GroupBox Grid.Row="3" Margin="5" Header="Selected">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Id:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SelectedIdText" Margin="0,0,10,6" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Type:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="3" x:Name="SelectedTypeText" Margin="0,0,0,6" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Name:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="NameBox" Margin="0,0,10,6"/>
                <CheckBox Grid.Row="1" Grid.Column="2" x:Name="ActiveBox" Content="Active" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <Button Grid.Row="1" Grid.Column="3" Content="Apply Name/Active" Height="24" Click="ApplyNameActive_Click" Margin="0,0,0,6"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Tag:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="TagBox" Margin="0,0,10,6"/>
                <TextBlock Grid.Row="2" Grid.Column="2" Text="Layer:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="2" Grid.Column="3" Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBox x:Name="LayerBox" Width="80" Margin="0,0,10,0"/>
                    <Button Content="Apply Tag/Layer" Height="24" Click="ApplyTagLayer_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Local Pos:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,0,10,6">
                    <TextBox x:Name="PosXBox" Width="70" Margin="0,0,6,0"/>
                    <TextBox x:Name="PosYBox" Width="70" Margin="0,0,6,0"/>
                    <TextBox x:Name="PosZBox" Width="70"/>
                </StackPanel>
                <TextBlock Grid.Row="3" Grid.Column="2" Text="Local Rot:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="3" Grid.Column="3" Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBox x:Name="RotXBox" Width="70" Margin="0,0,6,0"/>
                    <TextBox x:Name="RotYBox" Width="70" Margin="0,0,6,0"/>
                    <TextBox x:Name="RotZBox" Width="70"/>
                </StackPanel>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Local Scale:" Margin="0,0,6,6" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,0,10,6">
                    <TextBox x:Name="ScaleXBox" Width="70" Margin="0,0,6,0"/>
                    <TextBox x:Name="ScaleYBox" Width="70" Margin="0,0,6,0"/>
                    <TextBox x:Name="ScaleZBox" Width="70"/>
                </StackPanel>
                <Button Grid.Row="4" Grid.Column="3" Content="Apply Transform" Height="24" Click="ApplyTransform_Click" Margin="0,0,0,6"/>

                <WrapPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,0,0,6">
                    <TextBlock Text="Component Type:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="AddComponentBox" Width="260" Margin="0,0,10,0"/>
                    <Button Content="Add Component" Width="120" Margin="0,0,10,0" Click="AddComponent_Click"/>
                    <CheckBox x:Name="ComponentEnabledBox" Content="Enabled" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <Button Content="Apply Enabled" Width="120" Margin="0,0,10,0" Click="ApplyEnabled_Click"/>
                    <Button Content="Remove Selected" Width="130" Margin="0,0,10,0" Click="RemoveSelected_Click"/>
                    <Button Content="Duplicate" Width="110" Margin="0,0,10,0" Click="Duplicate_Click"/>
                    <Button Content="Destroy" Width="110" Margin="0,0,10,0" Click="Destroy_Click"/>
                    <Button Content="Despawn" Width="110" Click="Despawn_Click"/>
                </WrapPanel>

                <WrapPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,2,0,0">
                    <TextBlock Text="Parent Id:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="ParentIdBox" Width="160" Margin="0,0,10,0"/>
                    <Button Content="Apply Parent" Width="120" Margin="0,0,10,0" Click="ApplyParent_Click"/>
                    <Button Content="Unparent" Width="110" Margin="0,0,10,0" Click="Unparent_Click"/>
                    <TextBlock Text="Sibling Index:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="SiblingIndexBox" Width="70" Margin="0,0,10,0"/>
                    <Button Content="Apply Sibling" Width="120" Margin="0,0,10,0" Click="ApplySibling_Click"/>
                    <TextBlock Text="New:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="NewNameBox" Width="160" Margin="0,0,10,0"/>
                    <Button Content="Create Child" Width="120" Margin="0,0,10,0" Click="CreateChild_Click"/>
                    <Button Content="Create Root" Width="120" Click="CreateRoot_Click"/>
                </WrapPanel>
                </Grid>
            </ScrollViewer>
        </GroupBox>

        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Foreground="{DynamicResource PL.TextSecondaryBrush}" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
