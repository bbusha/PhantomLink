<UserControl x:Class="PhantomLink.GUI.PropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Search Controls -->
        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="SearchBox" Width="200" Margin="0,0,5,0" TextChanged="SearchBox_TextChanged"/>
            <TextBlock Text="Type Filter:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <TextBox x:Name="TypeFilterBox" Width="150" Margin="0,0,5,0"/>
            <Button Content="Search" Click="Search_Click" Margin="0,0,5,0"/>
            <CheckBox x:Name="AutoSearchCheckBox" Content="Auto" IsChecked="True" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <Button Content="Refresh Assemblies" Click="RefreshAssemblies_Click"/>
        </StackPanel>
        
        <!-- Property Filters -->
        <StackPanel Grid.Row="1" Margin="5" Orientation="Horizontal">
            <CheckBox x:Name="ReadableCheckBox" Content="Readable" IsChecked="True" Margin="0,0,10,0"/>
            <CheckBox x:Name="WritableCheckBox" Content="Writable" IsChecked="True" Margin="0,0,10,0"/>
            <CheckBox x:Name="StaticCheckBox" Content="Static" IsChecked="True" Margin="0,0,10,0"/>
            <CheckBox x:Name="PublicCheckBox" Content="Public" IsChecked="True" Margin="0,0,10,0"/>
        </StackPanel>
        
        <!-- Properties Grid -->
        <DataGrid x:Name="PropertiesGrid" Grid.Row="2" Margin="5"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Property Name" Binding="{Binding PropertyName}" Width="150"/>
                <DataGridTextColumn Header="Declaring Type" Binding="{Binding DeclaringType}" Width="200"/>
                <DataGridTextColumn Header="Property Type" Binding="{Binding PropertyType}" Width="100"/>
                <DataGridCheckBoxColumn Header="Can Read" Binding="{Binding CanRead}" Width="60"/>
                <DataGridCheckBoxColumn Header="Can Write" Binding="{Binding CanWrite}" Width="60"/>
                <DataGridCheckBoxColumn Header="Static" Binding="{Binding IsStatic}" Width="50"/>
                <DataGridCheckBoxColumn Header="Public" Binding="{Binding IsPublic}" Width="50"/>
                <DataGridTemplateColumn Header="Actions" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Get" Click="GetProperty_Click" Margin="2" Width="40"
                                        IsEnabled="{Binding CanRead}"/>
                                <Button Content="Set" Click="SetProperty_Click" Margin="2" Width="40"
                                        IsEnabled="{Binding CanWrite}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="PropertyCountText" Text="Properties: 0" Foreground="{DynamicResource PL.TextSecondaryBrush}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
