<Window x:Class="PhantomLink.GUI.RealtimeSceneExplorerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PhantomLink.GUI"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Realtime Scene Explorer" Height="780" Width="1280"
        WindowStartupLocation="CenterOwner"
        KeyDown="Window_KeyDown"
        KeyUp="Window_KeyUp">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Session:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock x:Name="SessionText" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <TextBlock x:Name="ModeText" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,12,0"/>
                <Button Content="Close" Width="110" Click="Close_Click"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="#2A2F36" CornerRadius="6" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Viewport3D x:Name="Viewport"
                                Grid.Row="0"
                                Focusable="True"
                                MouseDown="Viewport_MouseDown"
                                MouseUp="Viewport_MouseUp"
                                MouseMove="Viewport_MouseMove">
                        <Viewport3D.Camera>
                            <PerspectiveCamera x:Name="Camera" Position="0,2,-10" LookDirection="0,-0.2,1" UpDirection="0,1,0" FieldOfView="60"/>
                        </Viewport3D.Camera>
                        <ModelVisual3D>
                            <ModelVisual3D.Content>
                                <Model3DGroup x:Name="SceneRoot">
                                    <AmbientLight Color="#404040"/>
                                    <DirectionalLight Color="#FFFFFF" Direction="-0.3,-1,0.2"/>
                                </Model3DGroup>
                            </ModelVisual3D.Content>
                        </ModelVisual3D>
                    </Viewport3D>

                    <DockPanel Grid.Row="1" Margin="0,6,0,0" LastChildFill="True">
                        <TextBlock x:Name="ViewportHintText" Foreground="Gray" DockPanel.Dock="Left" Text="RMB: look  |  WASD: move  |  W/E/R: move/rotate/scale  |  LMB: pick"/>
                        <TextBlock x:Name="ViewportStatusText" Foreground="Gray" DockPanel.Dock="Right"/>
                    </DockPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" BorderThickness="1" BorderBrush="#2A2F36" CornerRadius="6" Padding="10" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Selection" FontWeight="SemiBold"/>
                        <TextBlock x:Name="SelectedText" Foreground="Gray"/>
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Components" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <ListBox x:Name="ComponentsList" Height="118" SelectionChanged="ComponentsList_SelectionChanged" MouseDoubleClick="ComponentsList_MouseDoubleClick">
                                <ListBox.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Inspect" Click="InspectSelectedComponent_Click"/>
                                        <Separator/>
                                        <MenuItem Header="Pin Object" Click="PinSelectedComponent_Click"/>
                                        <MenuItem Header="Unpin Object" Click="UnpinSelectedComponent_Click"/>
                                        <Separator/>
                                        <MenuItem Header="Dump to JSON" Click="DumpSelectedComponent_Click"/>
                                    </ContextMenu>
                                </ListBox.ContextMenu>
                            </ListBox>
                            <DockPanel Margin="0,8,0,0" LastChildFill="True">
                                <CheckBox x:Name="ComponentEnabledCheckBox" Content="Enabled" DockPanel.Dock="Left" Click="ComponentEnabled_Click"/>
                                <TextBlock x:Name="ComponentEnabledStatus" Foreground="Gray" Margin="10,2,0,0"/>
                            </DockPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Bottom">
                            <Button Content="Open Type Inspector" Width="160" Click="OpenTypeInspector_Click" Margin="0,0,0,8"/>
                            <Button Content="Refresh" Width="160" Click="RefreshSelection_Click"/>
                        </StackPanel>
                    </Grid>

                    <TabControl Grid.Row="2" x:Name="InspectorTabs">
                        <TabItem Header="Hierarchy">
                            <local:SceneHierarchyView x:Name="HierarchyView"/>
                        </TabItem>
                        <TabItem Header="Transform">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="World Pos" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBox x:Name="WorldPosBox" Grid.Column="1"/>
                                </Grid>

                                <Grid Grid.Row="1" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="World Rot" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBox x:Name="WorldRotBox" Grid.Column="1"/>
                                </Grid>

                                <Grid Grid.Row="2" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Local Scale" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBox x:Name="LocalScaleBox" Grid.Column="1"/>
                                </Grid>

                                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
                                    <Button Content="Apply" Width="90" Margin="0,0,10,0" Click="ApplyTransform_Click"/>
                                    <Button Content="Reset" Width="90" Click="ResetTransform_Click"/>
                                </StackPanel>

                                <TextBlock Grid.Row="4" x:Name="TransformStatus" Foreground="Gray" />
                            </Grid>
                        </TabItem>
                        <TabItem Header="Fields">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                    <Button Content="Load" Width="90" Margin="0,0,10,0" Click="LoadFields_Click"/>
                                    <Button Content="Apply Selected" Width="130" Click="ApplyField_Click"/>
                                </StackPanel>
                                <DataGrid Grid.Row="1" x:Name="FieldsGrid" AutoGenerateColumns="False" IsReadOnly="False" SelectionMode="Single">
                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Watch Value" Click="WatchSelectedField_Click"/>
                                            <MenuItem Header="Copy Full Path" Click="CopySelectedFieldPath_Click"/>
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="200" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <TextBlock Grid.Row="2" x:Name="FieldsStatus" Foreground="Gray" Margin="0,6,0,0"/>
                            </Grid>
                        </TabItem>
                        <TabItem Header="Properties">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                    <Button Content="Load" Width="90" Margin="0,0,10,0" Click="LoadProperties_Click"/>
                                    <Button Content="Apply Selected" Width="130" Click="ApplyProperty_Click"/>
                                </StackPanel>
                                <DataGrid Grid.Row="1" x:Name="PropertiesGrid" AutoGenerateColumns="False" IsReadOnly="False" SelectionMode="Single">
                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Watch Value" Click="WatchSelectedProperty_Click"/>
                                            <MenuItem Header="Copy Full Path" Click="CopySelectedPropertyPath_Click"/>
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="200" IsReadOnly="True"/>
                                        <DataGridCheckBoxColumn Header="Read" Binding="{Binding CanRead}" Width="55" IsReadOnly="True"/>
                                        <DataGridCheckBoxColumn Header="Write" Binding="{Binding CanWrite}" Width="55" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <TextBlock Grid.Row="2" x:Name="PropertiesStatus" Foreground="Gray" Margin="0,6,0,0"/>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" x:Name="StatusText" Foreground="Gray" Margin="0,10,0,0"/>
    </Grid>
</Window>
