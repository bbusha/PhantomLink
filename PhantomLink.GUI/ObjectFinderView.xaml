<UserControl x:Class="PhantomLink.GUI.ObjectFinderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal">
            <TextBlock Text="Type:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="TypeBox" Width="260" Margin="0,0,10,0" Text="UnityEngine.GameObject"/>
            <TextBlock Text="Name/Id Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="FilterBox" Width="220" Margin="0,0,10,0"/>
            <Button Content="Find" Width="90" Margin="0,0,10,0" Click="Find_Click"/>
            <Button Content="Refresh Selected Info" Width="170" Click="RefreshInfo_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="ResultsGrid" Margin="5"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  MouseDoubleClick="ResultsGrid_MouseDoubleClick">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Inspect" Click="InspectSelected_Click"/>
                    <Separator/>
                    <MenuItem Header="Pin Object" Click="PinSelected_Click"/>
                    <MenuItem Header="Unpin Object" Click="UnpinSelected_Click"/>
                    <Separator/>
                    <MenuItem Header="Dump to JSON" Click="DumpSelected_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tracked Id" Binding="{Binding TrackedId}" Width="120"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding Active}" Width="70"/>
                <DataGridTemplateColumn Header="Actions" Width="170">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Info" Width="70" Margin="2" Click="Info_Click"/>
                                <Button Content="Inspect" Width="70" Margin="2" Click="Inspect_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Foreground="{DynamicResource PL.TextSecondaryBrush}" Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="DetailsText" Foreground="{DynamicResource PL.TextSecondaryBrush}" Text=""/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
