<UserControl x:Class="PhantomLink.GUI.HierarchyBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal">
            <Button Content="Load Tree" Width="110" Margin="0,0,10,0" Click="Load_Click"/>
            <CheckBox x:Name="IncludeUnityCheckBox" Content="Include Unity types" IsChecked="True" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Find Type:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="FindBox" Width="220" Margin="0,0,10,0"/>
            <Button Content="Open Inspector" Width="130" Click="OpenInspector_Click"/>
        </StackPanel>

        <TreeView Grid.Row="1" x:Name="Tree" Margin="5"/>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Foreground="{DynamicResource PL.TextSecondaryBrush}" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
